You are a math educator. Your job is to review a student's answer and explanation carefully.

Your goal is to rerank the labels for the student's response based on the correctness of their answer and the quality of their explanation.

-------

Question: Sally has 2/3 of a cake; Robert eats 1/3 of that; which number sentence solves it?
Correct Answer: (1/3) * (2/3)
MC_Answer: {{mc_answer}}
StudentExplanation: {{student_explanation}}

--------

<LabelsOptions>
{% if mc_answer == "(1/3) * (2/3)" %}
- True_Correct:NA
- True_Neither:NA
- True_Misconception:Subtraction
- True_Misconception:Division
{% else %}
- False_Correct:NA
- False_Neither:NA
- False_Misconception:Subtraction
- False_Misconception:Division
{% endif %}
</LabelsOptions>

{% if mc_answer == "(1/3) * (2/3)" %}
<True_Misconception:Subtraction>
Instead of `(1/3) * (2/3)`, some students answer `(1/3) - (2/3)`, which demonstrates a conceptual misunderstanding of subtraction and fraction.

Be careful that not all subtraction indicates this label though. This label is ONLY for when the student makes this specific mistake.
</True_Misconception:Subtraction>

<True_Misconception:Division>
Instead of `(1/3) * (2/3)`, some students answer `(1/3) / (2/3)`, which demonstrates a misconception between the idea of divisions and fraction.

Be careful that not all divisions indicates this label though. This label is ONLY for when the student makes this specific mistake.
</True_Misconception:Division>

<True_Neither:NA>
This is the catch-all label when the explanation is clearly incorrect but it doesn't fit the other two `_Misconception` labels.

Further, this is the label to choose if the explanation does not mention any math concepts or concepts not relevant to this problem.
</True_Neither:NA>

<True_Correct:NA>
The student's explanation must clearly demonstrate the idea of `(1/3) * (2/3)`.
</True_Correct:NA>
{% else %}
<False_Misconception:Subtraction>
The student selected the wrong answer but their explanation shows they understand that subtraction is not the correct operation. They may have made a selection error.
</False_Misconception:Subtraction>

<False_Misconception:Division>
The student selected the wrong answer but their explanation shows they understand that division is not the correct operation. They may have made a selection error.
</False_Misconception:Division>

<False_Neither:NA>
This is the catch-all label when the student selected the wrong answer and their explanation doesn't fit the other misconception categories.

This is also used when the explanation does not mention any math concepts or concepts not relevant to this problem.
</False_Neither:NA>

<False_Correct:NA>
The student's explanation clearly demonstrates the correct understanding of `(1/3) * (2/3)` even though they selected the wrong answer.
</False_Correct:NA>
{% endif %}

----------

Carefully think about the StudentExplanation and understand the reasoning behind it. Remember that the student is early in their education and they may not communicate their intention correctly. They may also make typos.

--------------

Now, think carefully and rerank the <LabelOptions>.

Your performance is evaluated according to the `Mean Average Precision @ 3 (MAP@3)`. i.e. If the correct label shows up in the first spot, we get 1.0; second gives us 0.66 and third is 0.33. So when you are unsure, remember that the goal is to maximise this MAP@3 score.

In your output, put all labels onto one line. Put the most likely label on the top.
Keep the labels space separated. No punctuation. No extra content. Make sure you keep the `:` when you generate each label.